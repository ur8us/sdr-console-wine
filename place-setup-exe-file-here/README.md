# Put the SDR Console installer here

Download the desired 64-bit SDR Console Windows installer from its official
source and place exactly one `.exe` file in this directory.

The installer binary is intentionally ignored by Git and is never redistributed
by this project. Run `../setup.sh --dry-run` to verify that the script finds the
file before starting installation.
